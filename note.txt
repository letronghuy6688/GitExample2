***** clone một reponstory trên git về :
đứng tại thư mục muốn colne về rồi tạo teminal git clone (url của reponstory đó)

cách pull code về khio gặp sung đột có ng cùng sửa file 
git pull nếu vẫn gặp lỗi thì dùng thêm  git pull --no-rebase Hợp nhất các thay đổi từ nhánh remote(online) vào nhánh local

chỉnh sửa dưới local rồi commit len trên git 

chỉnh sửa trực tiếp trên git 
